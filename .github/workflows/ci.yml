name: CI

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main

jobs:
  sqlite-tests:
    name: Style + Tests (SQLite)
    runs-on: ubuntu-latest
    env:
      DJANGO_SETTINGS_MODULE: backend.foodgram.settings
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.14'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          cd backend
          pip install -r requirements.txt

      - name: 'Lint: isort (check)'
        run: isort --check-only .

      - name: 'Lint: flake8'
        run: flake8 .

      - name: 'Django: makemigrations check'
        # env:
        #   DJANGO_SETTINGS_MODULE: backend.foodgram.settings
        run: |
          python manage.py makemigrations --check --dry-run

      - name: 'Django: migrate'
        # env:
        #   DJANGO_SETTINGS_MODULE: backend.foodgram.settings
        run: |
          python manage.py makemigrations
          python manage.py migrate --noinput

      - name: Run tests (pytest)
        # env:
        #   DJANGO_SETTINGS_MODULE: foodgram_project.settings
        run: |
          pytest -q

  # postgres-checks:
  #   name: PostgreSQL integration checks (optional)
  #   runs-on: ubuntu-latest
  #   needs: sqlite-tests
  #   services:
  #     postgres:
  #       image: postgres:18
  #       env:
  #         POSTGRES_DB: test_db
  #         POSTGRES_USER: postgres
  #         POSTGRES_PASSWORD: postgres
  #       ports:
  #         - 5432:5432
  #       options: >-
  #         --health-cmd="pg_isready -U postgres"
  #         --health-interval=10s
  #         --health-timeout=5s
  #         --health-retries=5
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4

  #     - name: Set up Python
  #       uses: actions/setup-python@v4
  #       with:
  #         python-version: '3.14'

  #     - name: Install dependencies
  #       run: |
  #         python -m pip install --upgrade pip
  #         cd backend
  #         pip install -r requirements.txt
  #         pip install psycopg2-binary

  #     - name: Configure DB env
  #       env:
  #         DATABASE_URL: postgres://postgres:postgres@localhost:5432/test_db
  #         DJANGO_SETTINGS_MODULE: backend.foodgram.settings

  #       run: |
  #         python manage.py makemigrations --check --dry-run
  #         python manage.py migrate --noinput
  #         pytest -q
